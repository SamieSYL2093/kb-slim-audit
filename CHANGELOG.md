# CHANGELOG

> 用途：对外发版的变更清单。每次发新版在顶部插入新条目，Release 说明直接引用本文件。

## [1.5.4] - 2026-08-29（已发布）

### 修复（流程缺口——审计遗留项中唯一带泄漏风险的）
- **发布 SOP 补「新仓无历史」**：文件级脱敏只护住工作区文件；git 历史的提交信息里仍含脱敏前的人名与内部编号——连带历史 push 即全漏。`AGENTS.md` 发布节明确：对外建仓 = `publish/` 复制出去后**全新 `git init`**，严禁带本仓历史发布
- `publish_prep.py` 生成成功的输出加同款提醒（脚本出口即 SOP 出口）

### 涉及文件
`AGENTS.md`、`scripts/publish_prep.py`、`SKILL.md`（版本号）、`CHANGELOG.md`、`publish/`（重跑生成）

---
## [1.5.3] - 2026-08-29

对齐「宣称 ≠ 实现」的剩余缺口（Kimi Work 审核 P1 后半）。

### 新增
- **S10 台账流水**：台账类文件中日期开头行 ≥10 且占比 >30% → 判定式 10 从"宣称机检"变成"真有规则"；同时消除与 S2 台账豁免的冲突（S2 不管台账，S10 专管台账的流水）
- **findings 对齐 §十四 契约**：每条带 `规则ID + 严重度 + 置信度 + 类别 + 位置 + 证据 + 建议动作`；`--json` 输出含 `rule`/`confidence` 字段，可直接喂 triage 横向比召回
- 自检脚本加**契约断言**：任何 finding 缺规则 ID 或置信度即失败
- fixtures +2（台账流水正例 / 干净台账反例），expected.json 扩到 5 应报 + 3 不误报

### 宣称对齐（§十三）
- 表格加「对应机检」列：机检必须指明对应规则，宣称即实现
- #6 判定式短句、#13 下沉后只留一行指针：机检 → **候选**（无对应检测，"长文阈值/半截说明"需语义判断）
- 统计随之更正：**机检 6 / 候选 11 / 手动 4**（机检+候选仍占 17/21）

### 涉及文件
`scripts/kb_slim_scan.py`、`scripts/kb_slim_selftest.py`、`tests/fixtures/`（+2 样本）、`SKILL.md`、`CHANGELOG.md`、`publish/`（重跑生成）

---
## [1.5.2] - 2026-08-29

落地自家 §十四 第 3 条：**可回归测试**——"真空绿"的正解。语料即 1.5.1 审核实证用的配方。

### 新增
- `tests/fixtures/`：7 个已知问题样本（多行头注堆叠、单行双日期、真孤儿、两个被文字提及的文件、编号表格中心文档、复述稿）——**故意埋的问题，勿"修复"**（`tests/README.md` 有警示）
- `tests/fixtures/expected.json`：断言表——应报 4 条 + 不应误报 2 条 + 中心文档关键词提取下限；加新案例 = 放样本 + 加一行断言，不动代码
- `scripts/kb_slim_selftest.py`：回归自检，全过退出码 0、否则 1；改完 `kb_slim_scan.py` 必跑

### 变更
- `kb_slim_scan.py` 的 `SKIP_DIRS` 补 `tests`——否则自扫本仓时 fixture 里故意埋的问题会淹没真实报告
- `publish_prep.py` 白名单补 `tests/`——发布的仓库应自带"证明检出能力在线"的样本，否则 SKILL.md 里的自检指引成了悬空指针

### 涉及文件
`tests/`、`scripts/kb_slim_selftest.py`、`scripts/kb_slim_scan.py`、`scripts/publish_prep.py`、`SKILL.md`、`AGENTS.md`、`CHANGELOG.md`、`publish/`（重跑生成）

---
## [1.5.1] - 2026-08-29

跨 AI 审核（Kimi Work）用「已知问题样本」实测脚本检出能力后的修复轮——三个 bug 均先实证复现，修复后用同样本回归验证。

### 修复（扫描脚本，均经实证复现）
- **S8 孤儿检测逻辑反转**：文件名被提及时，错把「提及别人的文件」标为被引用——真孤儿漏报、被提及者误报为孤儿；顺带修复该双重循环对每个文件对重复读盘（复用已缓存文本，约 n² 次磁盘读取消除）
- **S5 头注堆叠漏检**：原只认「单行 ≥2 个日期」，漏掉最常见的「多行 `> 更新` 各带一个日期」；改为文件头 15 行内命中 ≥2 行即报（单行多日期仍报）
- **S3 复述检测对编号表格静默失效**：关键词正则只抓表格首列，`| 1 | **术语** |` 抓到序号被过滤 → 0 关键词 → S3 整体关闭；改为表格行优先抓整行加粗词、无加粗退回首列。状态消息区分「未找到中心文档」与「找到但提取 0 词」（后者明示疑似真空绿）

### 修复（数字漂移——撞上自家判定式 21）
- 1.5.0 条目裁决统计「建议 6 条、必须人拍板 13 条」→ 按 §十三 表逐条数实为 **5 / 14**，本条已改
- §十三「约 1/3 机检、1/3 候选、1/3 需要读」→ 实为 **8 / 9 / 4**（需要读仅约 1/5）
- §十一 S5 规则描述同步为新检出口径

### 已知待办（本次未含）
- §十三 标「机检」的 8 条中，#6 / #10 / #13 尚无对应脚本规则（宣称与实现的对齐留待下版；#10 与 S2 台账豁免的冲突同理）——**→ 1.5.3 已落地**（S10 新增；#6/#13 降候选）
- 回归测试语料（tests/fixtures）未建——本次审核用的「已知问题样本」即雏形，见 §十四 第 3 条——**→ 1.5.2 已落地**
- 发布 SOP 待补「新仓从 publish/ 全新 git init、不带历史」（git 历史含脱敏前信息，连带历史 push 即泄漏）——**→ 1.5.4 已落地**

### 涉及文件
`scripts/kb_slim_scan.py`、`SKILL.md`、`CHANGELOG.md`、`publish/`（重跑生成）

---
## [1.5.0] - 2026-08-28

回答"这么多检查内容怎么管"的三个问题：分层、半自动、统一流程。

### 新增 §十二 分层审查
借用汽车业**分层过程审核（LPA / AIAG CQI-8）**——同一件事由不同层级、按不同频率各查一遍：

| 层 | 何时 | 谁 | 查什么 |
|---|---|---|---|
| L1 机检 | 每次改动 | 钩子/脚本 | 结构类：死链、孤儿、编码、脱敏、孤本引用 |
| L2 语义 | 每次任务 | AI | 21 条判定式 |
| L3 体系 | 月/季 | 人 + AI | 口径是否传到所有仓、制度是否还在起作用 |

抄了 LPA 两条经验：① 多数失败不是未知风险，是**已知风险没被一致地控制**；② 清单本身不值钱，**一致的观察 + 快速纠偏**才值钱。防"打勾敷衍"靠钩子强制 + 空清单也要声明查过 N 条。

### 新增 §十三 半自动：检出与裁决分离
核心洞察：**说"这条没法自动化"，通常是裁决没法自动，但检出可以。**
半自动 = 机器出候选（召回优先）+ 人裁决（精度靠人）+ 每条给足证据。

给 21 条判定式逐条标了「检出方式 × 裁决方式」，统计结果：
- **8 条可机检、9 条能出候选、4 条需要读**（需要读仅约 1/5）
- 裁决侧：自动 2 条、建议 5 条、必须人拍板 14 条（v1.5.1 修正：原写 6 / 13，与 §十三 表不符）

并接上精益的 **自働化（Jidoka）**：异常自动检出 → 自动停止 → 呼人处理。**判定权留给人是设计，不是缺陷。**

### 新增 §十四 统一流程：统一契约，不统一实现
固定五步的输入输出，不同 AI 方法不同也没关系：

```
scan → detect → triage → apply → verify
```

每步产物必须包含的字段（findings 要带规则 ID + 位置 + 证据 + 置信度）。
三个好处：人只在 triage 打勾；不同 AI 的 findings 可横向比召回；**可回归测试**（攒已知问题样本，改完脚本验证还报得出来——"真空绿"的正解）。

### 补漏
- 判定式 17 补上 **ROT 分析里的 T（Trivial）**：内容治理标准三分是 Redundant / Outdated / Trivial，本 skill 原本只覆盖了前两类。判据：**想象它消失了，谁会来找？**

### 涉及文件
`SKILL.md`、`CHANGELOG.md`

---
## [1.4.2] - 2026-08-28

### 新增：发布时才脱敏（用户 2026-08-28 定）
> 制作/升级过程一直惦记脱敏，会拖慢内容打磨。脱敏改为**发布时的一次性动作**。

- `scripts/publish_prep.py`：从本体生成项目内待发布副本 `publish/`
  - 按白名单复制（`SKILL.md`/`README.md`/`LICENSE`/`CHANGELOG.md`/`scripts/`/`references/`），自动排除 `AGENTS.md` 与立项文档
  - **自动脱敏 + 复检**：替换完再扫一遍，有残留就**阻断**并列出位置
- 映射表 `.sensitive-patterns.json`：**一张表两用**——扫描检查与自动替换共用，不会漏
  - 私有词（人名 / AI 代号）走配置，通用规则（绝对路径 / 编号格式）内置在脚本
  - 脚本不写私有词字面 → 规则表自己不会成为泄漏点
- `publish/` 落 git：副本可 diff、可审查，但标明是**生成物**（改本体再重跑）

### 修复
- 扫描脚本 `SKIP_DIRS` 补 `publish`——否则待发布副本会被当成"重复文件"误报
- 脚本不再 `rmtree`（沙箱安全钩子会拦），改为覆盖写 + 陈旧文件提示

### 涉及文件
`scripts/publish_prep.py`、`.sensitive-patterns.json`、`scripts/kb_slim_scan.py`、`AGENTS.md`、`CHANGELOG.md`、`publish/`

---
## [1.4.1] - 2026-08-28

### 修复（发布面脱敏）
- `README.md` / `references/示例审查报告（脱敏）.md` 的示例里残留**内部工单编号** → 泛化为通用文件名
- `CHANGELOG.md` 历史条目含**人名**与**内部项目号** → 改为中性表述
  （注：描述修复时不复述原敏感串，否则补丁本身成了新的泄漏点）
- 教训：脱敏不能只扫正文，**示例、历史条目、参考文件同样在发布面内**

### 修复（一致性）
- `AGENTS.md` 定位行仍写"8 条判定式 + 五步工作流"，与正文 21 条脱节 → 同步
- `1.4.0` 条目「涉及文件」漏记 `scripts/kb_slim_scan.py`
- `README.md` 症状表指向"结构化"判定式，正文无此条 → 改指脚本规则 S2（消除悬空指针）

### 新增
- 反面案例库 +2 例，均取自本次审查自身：改口径漏掉不发布的文件、grep 报"全绿"但发布面仍泄漏
  —— 说明的是同一件事：**改完的标准是最后一处旧说法被替换，工具报绿不等于真的扫到了**

### 涉及文件
`SKILL.md`、`README.md`、`CHANGELOG.md`、`AGENTS.md`、`references/示例审查报告（脱敏）.md`

---
## [1.4.0] - 2026-08-28

### 新增
- **补漏判定式 +2**（源自多轮知识库治理实战复盘）：
  - **20 冻结不删**：机制已停 / 功能已废但有历史或设计参考价值 → 不删历史、不重启，决策点归档、留"想重启先读这里"的导航；处置词汇从五类扩为六类（删 / 并 / 压 / 移 / 指针化 / **冻结**）
  - **21 旧措辞残留**：口径演进后旧说法散在 N 个文件 → 全库 grep 旧措辞逐处替换；改完的标准是"最后一处旧说法被替换"
- **「砍 / 不砍 / 搬」三分边界**：判定式管"什么该瘦"，边界表管"什么不能碰"——不砍清单（活数据 / 判定依据 / 指针 / 历史档案）+ 搬家必带收口 + 低 ROI 项先冻结缓办
- **前置动作 +1：瘦身先核精神**——动刀前先读目标仓定位说明，理解核心精神再审查
- **历史档案豁免**：实录 / 记录类文档在扫描盘点步明确排除，其中过时引用也不追改
- **验收 +1：量化结果**——记录瘦身前后规模对比，"感觉瘦了"不算数
- **反面案例库 +2 例**：机制停用直接删文件没留冻结归档、口径演进后旧做法散在多处事实源

### 修复
- description / README 数字与正文脱节（"10 类补漏" / "18 条判定式" / "8 个案例"均已过时）——正是本 skill 警告的"一致性盲区"，本次一并修正

### 涉及文件
`SKILL.md`、`README.md`、`CHANGELOG.md`、`scripts/kb_slim_scan.py`

---
## [1.3.0] - 2026-08-28

### 新增
- **随附扫描脚本 `scripts/kb_slim_scan.py`**（零依赖纯标准库，跨平台）：机械可判的 9 类冗余（S1~S9）
  大文件 / 数据当文档 / 复述中心文档 / 同名聚类（同目录内）/ 头注堆叠 / 命名笼统 / 死链 / 孤儿 / 空占位与久挂待办
  支持 `--center`（指定复述基准）、`--big`（阈值）、`--top`、`--json`
- **文体三分**章节：规则要薄、清单要厚、给 AI 的指令只写领域特有的——三种文体要求相反，最易搞混
- **AI 盲区与归因**章节：
  - 五种系统性盲区（看不见自己刚写的 / 一致性盲区 / 新增偏好 / 格式惯性 / 过度信任工具）
  - **先归因再处置**：人的习惯靠原则与自觉（别用机制拦），AI 的失误用机制挡（hook/脚本/模板默认值）

### 涉及文件
`scripts/kb_slim_scan.py`、`SKILL.md`、`CHANGELOG.md`

---
## [1.2.0] - 2026-08-28

### 变更
- **删除「输出模板」整章**——实战反馈：报告怎么排版，每个做审查的 AI 都知道，不用规定。
  保留的是"每条必须包含什么信息"（位置 + 理由 + 建议动作）和验收口径，那是容易做错的
- 工作流第 5 步改为"输出可执行项清单，每条写清位置+理由+建议动作"，不再给格式样板
- `references/示例审查报告` 重新定位：不是"模板填好的样子"，而是**效果示例**（展示这套方法能挖出什么）

### 新增
- **补漏判定式第 19 条：不规定 AI 本来就会做的事**——输出模板、格式样板、给 AI 的排版指令一律删；
  只规定容易做错的、领域特有的事。判据：**删掉它，AI 会不会做错？不会 → 删**
- 反面案例库补 2 例：规范里挂输出模板（含 emoji 加粗级规定）、瘦身规范本身写太简导致漏掉十类冗余

### 涉及文件
`SKILL.md`、`references/示例审查报告（脱敏）.md`、`CHANGELOG.md`

---
## [1.1.0] - 2026-08-28

### 新增
- **补漏判定式 10 类**（9~18）：命名笼统=变相加层 / 台账不搬流水 / 头注不堆叠历史 / 操作细节不属于规范 / 下沉后只留一行指针 / 细则并入原则 / 规则单源化 / 去重复强调 / 无人用的删除 / 孤儿清理
  —— 源自一次全库复盘与全量 `git log` 回溯，八条核心之外的真实冗余源
- **三个前置动作**：动手前 `git log` 核实（旧记忆是线索不是证据）、确认有无 git、确认有无别人在改
- **防呆清单扩到 11 条**：改名删章节三件套（grep 引用 + .gitignore 同步 + `git ls-files` 查应忽略却被跟踪）、别顺手剪掉、别过度执行、机械防呆优于文字清单、警惕"真空绿"……
- **反面案例库 8 例**：误加规范文件、删章节留下游断链、双份立项文档、数据当文档存、本体与分发稿双维护、宣称的规则没实现、模板改了活仓没同步、顺带剪掉丢信息
- **验收章节**：说改≠改了（验收看 git log）、谁改谁签、逐项核
- README 新增"为什么这么厚"一节

### 变更
- 定位明确：**不做成极简清单**——漏掉一类冗余就等于白审一次，宁可厚一点兜住问题点

### 涉及文件
`SKILL.md`、`README.md`、`CHANGELOG.md`

---
## [1.0.0] - 2026-08-28（立项目标版本）

### 新增
- 首版立档：8 条判定式（单一事实来源 / 放对层 / 双写消除 / 旧版清退 / 合并语义重复 / 判定式短句 / 孤本保护 / 平台私有清退）
- 五步工作流：扫描盘点 → 复述检测 → 同名聚类 → 元文档逐读 → 落报告
- 可执行项输出模板（删 / 并 / 压 / 移 四类 + 「查过不动」备忘区）
- 防呆清单：孤本检查、无 git 仓先存档、删章节前 grep 下游引用、数据转结构化格式
- `README.md`（人看）、`AGENTS.md`（AI 开工须知）、`立项-知识库瘦身审查skill.md`（立项依据）

### 来源
方法论提炼自 2026-08-28 一次 22 仓 / 183 个 MD 的全库审查实践，实例已脱敏为通用表述。

### 涉及文件
`SKILL.md`、`README.md`、`AGENTS.md`、`CHANGELOG.md`、`LICENSE`、`立项-知识库瘦身审查skill.md`
