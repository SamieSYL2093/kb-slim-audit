---
slug: kb-slim-audit
name: kb-slim-audit
displayName: 知识库瘦身审查
description: 知识库/文档仓瘦身审查。用 8 条核心判定式 + 13 类补漏判定式审查 Markdown 文档，输出「删 / 并 / 压 / 移 / 指针化 / 冻结」可执行清单，并标注查过确认不动的文件。内置三层审查节奏（L1 机检 / L2 语义 / L3 体系）、检出与裁决分离、防呆清单与真实反面案例库。当用户要求审查文档冗余、精简知识库、瘦身 MD 仓、清理重复文档、判断文档能否删除或合并时使用。
version: 1.5.5
summary: 文档仓越用越胖？把本 skill 发给任意桌面端 AI，它按 21 条判定式把你的 Markdown 知识库过一遍，输出「删/并/压/移/指针化/冻结」可执行清单——查过不动的也标注，AI 只出清单、人拍板。附零依赖机检脚本 S1-S10（只报不修）与回归自检语料（防真空绿）。MIT，下载即用。
license: MIT
metadata:
  author: SamieSYL2093
---

# 知识库瘦身审查

给一批 Markdown 文档做"能删就删"的审查，**输出可执行清单而不是直接动手**——删什么由人拍板，审查由 AI 干。

> 本 skill 故意不做成极简清单。文档冗余的形态比想象中多，**漏掉一类就等于白审一次**，所以宁可厚一点，把实战踩到的问题点都兜住。

## 一、底层原理：为什么必须瘦

- **规范是"被引用"的，不是"被通读"的**——长文靠自觉必失败。写的人觉得"都有用"，读的人（AI）每次全量读入
- **文档越长，AI 每次读的越多、越贵、越慢**——token 成本随库长线性增长，且稀释重点
- **冗余不只是多余，它会腐化**：同一件事三处写着，改一处漏两处，三处互相矛盾，最后没人知道哪个对
- **规范只增不减是病**：文档天生只会长不会自己瘦——每加一条先看能砍什么；瘦身是周期性动作，不是一次性工程

## 二、核心八条判定式

按序过一遍，命中即出对应处置：

| # | 判定式 | 典型症状 | 处置 |
|---|--------|---------|------|
| 1 | **单一事实来源** | 同一知识出现在第二处 | 后者改**指针**（"见 X 原则 N"一句顶一段） |
| 2 | **放对层** | 内容不属于本仓职责（理念/细则/数据混放） | **下沉/上交**到它该在的仓 |
| 3 | **双写消除** | 同一数据两处维护，改一处漏一处 | **单点维护** + 脚本自动带入或机检对账 |
| 4 | **旧版清退** | 被机检取代的手工清单、已退役机制的残留 | **删** |
| 5 | **合并语义重复** | 两个条目说同一件事 | **合并**（保留更实的，并入对方独有信息） |
| 6 | **判定式短句** | 解释性长文、大段背景铺陈 | 压成**判定式短句 / 检查要点表** |
| 7 | **孤本保护** | 删之前没查别处有没有副本 | **全库 grep 引用**；孤本压缩不删 |
| 8 | **平台私有清退** | 平台自装自管/单端私有的条目混进共享台账 | **清出共享台账** |

## 三、补漏判定式：实战最常漏的 13 类

这 13 类是"八条之外"的高频冗余源，审查时单独过一遍：

| # | 判定式 | 症状 → 处置 |
|---|--------|-----------|
| 9 | **命名笼统 = 变相加层** | 「工具 / 案例 / 资料 / 其他」式统称会逼出下一层套娃 → 命名具体化，或解散该层 |
| 10 | **台账不搬流水** | 台账里堆日期进度流水 → 流水回项目仓 README/git log，台账只留定位与关键事实 |
| 11 | **头注不堆叠历史** | 文件头 `> 更新` 行堆了七八条历史 → 只留最近一次，历史交 `git log` |
| 12 | **操作细节不属于规范** | 规范里夹命令对照表、安装步骤、软件手册 → 外链或移交项目仓（这类内容能让规范瘦 70%+） |
| 13 | **下沉后只留一行指针** | 内容移走了，原处还留半截说明 → 原处只留一行指针，不复制摘要 |
| 14 | **细则并入原则** | 原则已说清，又单列一节细则复述 → 细则删，只留一句操作性解释 |
| 15 | **规则单源化** | 同一份协议/规则在多个文件各存一份 → 只在定义处维护，别处做转发卡（含指针） |
| 16 | **去重复强调** | 同一个意思换三种说法反复讲 → 留最有力的一种 |
| 17 | **无人用的删除 / 琐碎内容** | 写了但从没人用的功能、脚本、说明；**删了也不会有人来找的内容** → 直接删（git 历史留着）。判据：**想象它消失了，谁会来找？** |
| 18 | **孤儿清理** | 孤儿依赖目录、空占位文件、久挂的"待办"、死链与过时指针 → 清或兑现 |
| 19 | **不规定 AI 本来就会做的事** | 输出模板、格式样板、"报告按这个格式写"、给 AI 的排版指令 → 全删。**只规定容易做错的、领域特有的事**；怎么组织输出是模型自带能力 |
| 20 | **冻结不删** | 机制已停 / 功能已废，但有历史或设计参考价值 → **不删历史、不重启**；决策点归档一处，留一条"想重启先读这里"的导航（"用到再治理"的冻结同理） |
| 21 | **旧措辞残留** | 口径 / 措辞演进过，旧说法还散在 N 个文件 → **全库 grep 旧措辞逐处替换**；口径改完的标准是"最后一处旧说法被替换"，不是"新版写好了"。**脱敏类替换尤其注意：改动记录里不要复述原敏感串**，否则补丁本身成了新泄漏点 |

> 第 19 条最容易被忽略，也最容易"看起来很专业"。判据很简单：**删掉它，AI 会不会做错？** 不会 → 删。

### 砍 / 不砍 / 搬：三分边界（动刀前先对照）

判定式管"什么该瘦"，这张表管"什么不能碰"——审查时两边都要过：

| 处置 | 范围 |
|------|------|
| **砍** | 解释性文字、重复叙述、可恢复的细节（安装步骤 / 版本号 / 原文摘录——git log 里能找回的，删起来不手软） |
| **不砍** | 活数据（盘点表 / 签名表 / 登记行）、判定依据（防呆逻辑、退役条件、豁免条款）、指针（下沉后必留"去哪找"）、**历史档案**（实录 / 记录类文档——其中过时引用也不追改，留待巡检再评估） |
| **搬** | 不属于本层的内容 → 下沉 / 上交；搬家必带收口：源处留指针 + 新家建文件 + 全库 grep 让引用跟随 |

> 不必一次动完：ROI 低的项先**冻结**缓办（判定式 20），本次审查明确记下"查过、暂不动"即可。

## 四、工作流

### 四个前置动作（别省）

1. **动手前先 `git log` 核实**——旧记忆是线索不是证据。你以为的文件状态很可能是上周的
2. **确认目标仓有没有 git**——无 git 的仓删除即永久丢失，处置策略完全不同
3. **确认有没有别人正在改**——多 AI/多人协作时看 `git status` 与最近提交时间，避免撞车
4. **瘦身先核精神**——先读目标仓的定位说明（README / AGENTS / 立项文档），确认理解了"这仓是干什么的、核心精神是什么"再动刀；方向理解错了，砍的全是错的

### 五步审查

1. **扫描盘点**：列出目标 MD 的路径 / 行数 / 大小；排除 node_modules、archive、缓存、自动生成物；历史档案（实录 / 记录类文档）标记为豁免瘦身；无 git 的仓单独标记
2. **复述检测**：拿中心文档（档案 / 总纲 / 宪法类）的关键词 grep 子文档，命中 ≥2 个 = 疑似复述，逐个看是合法指针还是全文抄写
3. **同名聚类**：文件名前缀相同、或行数大小相近的做 diff——确认是旧版残留，还是职责不同的双文件
4. **逐条过判定式**：元文档（AGENTS / README / 规范 / 指引）逐个读，先过八条核心，再过十三类补漏
5. **落报告**：输出可执行项清单，每条写清**位置 + 理由 + 建议动作**。**查过不动的也要记录**，否则下次重复劳动且有误删风险

> 报告怎么排版不用规定——AI 自己会组织。要规定的是"每条必须包含什么信息"，以及下面第六节的验收口径。

## 五、防呆清单（审查时逐条自检）

- **孤本三查**：全库 grep 引用 → git 历史可回滚 → 别处有无副本。三者都没有的，压缩不删
- **改名/删章节三件套**：① grep 全仓下游引用 ② `.gitignore` 路径同步 ③ `git ls-files` 查"应忽略却被跟踪"的文件
- **无 git 的仓先存档**：转格式、归档、或确认数据已在他处，再谈删除
- **别顺手剪掉**：缺口常藏在"顺带剪掉"里——合并后逐项对照原文核一遍
- **别过度执行**：用户说"补 X"，别顺手加没说的 Y。为规范而新建规范文件是最常见的过度执行
- **多仓口径一致性**：模板改了只算一半，"模板 → 活仓同步"必须显式列为动作项，否则各方互以为别人会做
- **机制分人 / AI**：机械防呆（hook、脚本、模板默认值）优于文字长清单——把文字清单升级成机检，规范才能真正瘦下来
- **校验工具本身要验证真扫到了**：脚本报"全绿"时，抽查一个已知问题能否被抓出（警惕中文路径编码等问题导致的"真空绿"）
- **别为规范而新建文件**：能并入既有文件的，不新建
- **边界外的兜底说明删掉**：明确不适用某场景后，为该场景写的兜底与解释一并清理
- **删除后复跑校验**：删完跑该库的 lint / 校验脚本 + 全库死链检查

## 六、验收

- **说改 ≠ 改了**：验收看 `git log`，没提交 = 没做；验收范围要含"口径是否传到所有该到的仓"
- **谁改谁签**：简化也是改动，署名 commit
- **逐项核**：确认的人对照原文逐项核对，重点看有没有顺带剪掉
- **量化结果**：记录瘦身前后的规模对比（行数 / 字符数）——瘦身本身也要被验收，"感觉瘦了"不算数

## 七、反面案例库（真实踩过的坑）

| 案例 | 教训 |
|------|------|
| 为规范而新建了一个规范文件，后来发现纯属多余又删掉 | 别过度执行；新建前先问"能不能并入既有文件" |
| 删了某个章节，下游三个文件还在引用它 | 断链比冗余更伤；删章节前 grep 下游 |
| 同一立项写两份文档（A 有决策路径、B 有分工边界） | 合并时保留**两边独有信息**，不是留一份删一份 |
| 1776 行题库数据存在 MD 里，AI 逐行读 | 数据转 JSON；删前确认数据已在系统里（抽验） |
| skill 本体与对外分发稿双份维护，改了本体分发稿不跟 | 单源化：分发稿从本体生成 |
| 规范里宣称有 12 条检查规则，脚本实际只实现了 11 条 | 宣称≠实现；改规则表前先看脚本 |
| 模板改了，但 5 个活仓没同步，各方以为别人会做 | 同步动作必须显式列出 |
| 合并时把一段"看起来啰嗦"的实例剪掉了，那其实是唯一的操作范例 | 别顺手剪掉；合并后逐项核 |
| 规范里挂着「报告输出模板」，还细致到 emoji 和加粗——但报告怎么排版 AI 本来就会 | 不规定 AI 自带能力（判定式 19）；只规定"每条必须含什么信息" |
| 一份瘦身审查规范本身写得很简，结果漏掉了十类常见冗余 | 审查类文档的价值在**覆盖面**不在简洁；清单要厚，规则才要薄 |
| 机制停用后直接删了工作文件，没留冻结归档——后来想重启，设计依据与决策点全丢了 | 冻结不删（判定式 20）：不删历史、决策点归档、留"想重启先读这里"的导航 |
| 口径从"按日期建子目录"改成"扁平存放"，模板、活仓、台账多处事实源还写着旧做法 | 口径演进后全库 grep 旧措辞逐处替换（判定式 21）；改完的标准是最后一处旧说法被替换 |
| 把清单条数从"10 类"改成"13 类"，正文、描述、人看文档都改了，内部指针文件还写着"8 条" | 同上：口径一改就要全库 grep。**改的人最容易漏的恰恰是不发布的那几个文件** |
| 发布前用 grep 扫一遍敏感信息报"全绿"就发版，实际发布面还留着内部工单号与人名 | 工具报"全绿"时先抽查（真空绿）；中文环境下 shell grep 可能静默失效，换脚本语言复核一遍 |

## 八、与相邻机制的分工（避免重复建设）

| 机制 | 管什么 |
|------|--------|
| skill 巡检类规范 | skill 是否掉队（上游版本、市场同类） |
| 库结构 lint 脚本 | 库结构：矛盾 / 过时 / 孤立 / 缺口 |
| **本 skill** | **文档内容冗余：删 / 并 / 压 / 移 / 指针化 / 冻结** |

三者是"技能 / 结构 / 内容"三个层面，互不替代。

## 九、文体三分：写之前先分清写的是哪一种

同样的"精简"要求，用在不同文体上结果完全相反。这是最容易搞混的一条：

| 文体 | 要求 | 理由 |
|------|------|------|
| **规则 / 原则** | **薄**——判定式短句 | 读了要照做，长了没人读；规范是被引用、不是被通读的 |
| **审查清单 / 检查表** | **厚**——覆盖优先 | 漏掉一类就等于白审一次 |
| **给 AI 的操作指令** | **只写领域特有的** | 模型自带能力（怎么组织输出、怎么排版）不用教 |

> 实操判据：写每一段前问"**删掉它，AI 会不会做错？**"——不会做错的就是冗余。

## 十、AI 盲区与归因

### 为什么多 AI 交叉审核仍有漏网的

即便让多个模型互相修改、互相审核，以下问题仍反复出现——说明这是**系统性盲区**，不是某个模型不够强：

| 盲区 | 表现 |
|------|------|
| **看不见自己刚写的** | 刚删完别处的输出模板，转头在自己新文档里又写一个 |
| **一致性盲区** | 改了 A 处口径，散在 B/C/D 处的旧措辞没跟着走 |
| **新增偏好** | 倾向"新建一份规范"而不是"并入既有文件" |
| **格式惯性** | 模仿邻近文件的写法，连同其中的冗余一起抄过来 |
| **过度信任工具** | 校验脚本报"全绿"就信，没验证它真的扫到了（真空绿） |

**应对**：盲区靠流程补，不靠模型变强——先机检后人工、删前查孤本、改后跑校验、查过不动也要记录。

### 先归因：是人的问题，还是 AI 的问题

处置方式完全不同，误判的代价都很实在：

| 归属 | 典型 | 该怎么处理 |
|------|------|-----------|
| **人的问题** | 命名堆叠（最终版/副本/v2）、习惯性散放文件 | 靠原则与自觉 + 顺手清理。**别用机制去拦** |
| **AI 的问题** | 编码污染、新建项目忘加 AGENTS、双写漂移、移文件留下游断链 | **用机制挡**：hook、脚本、模板默认值 |

> 把人的习惯当 AI 失误去拦 = 过度设计，惹人烦；
> 把 AI 失误当人的习惯放任 = 同一个坑反复踩。
> 判据：**这条错误是"不这么做就会发生"，还是"人就是这么习惯"？**

## 十一、自动化：随附扫描脚本

`scripts/kb_slim_scan.py` 覆盖**机械可判**的部分（零依赖，纯标准库，跨平台）：

```bash
python scripts/kb_slim_scan.py ./docs            # 扫指定目录
python scripts/kb_slim_scan.py ./docs --top 15   # 调大显示条数
python scripts/kb_slim_scan.py ./docs --center X-NN_档案.md   # 指定复述检测基准
python scripts/kb_slim_scan.py ./docs --json     # JSON 输出（含 rule/confidence，对齐 §十四 findings 契约）
```

| 规则 | 检什么 |
|------|--------|
| S1 大文件 | 行数超阈值（默认 300） |
| S2 数据当文档 | 数据行占比 > 60%（台账/清单类豁免——表格是它们的本职；台账的冗余由 S10 管） |
| S3 复述中心文档 | 命中中心文档术语 ≥2 个 |
| S4 同名聚类 | **同一目录内**同前缀文件（跨目录的 README/AGENTS 是标配，不算） |
| S5 头注堆叠 | 文件头 `> 更新` 类行 ≥2 行（或单行堆多个日期） |
| S6 命名笼统 | 目录名是泛词（工具/资料/案例/其他） |
| S7 死链 | md 相对链接指向不存在的文件 |
| S8 孤儿文件 | 未被任何 md 引用（中心文档除外） |
| S9 空占位 / 久挂待办 | 几乎无内容，或堆了 ≥3 处 TODO |
| S10 台账流水 | 台账类文件中日期开头行 ≥10 且占比 >30%（流水回项目仓，台账只留定位与关键事实） |

**脚本做不到的**（必须人/AI 判断）：语义重复、放对层、细则并入原则、去重复强调、规则单源化、无人用的删除。

> 脚本也会有自己的盲区——写完先拿已知有问题的目录试一遍，看它能不能报出来。**报"全绿"时尤其要抽查**（警惕真空绿）。本仓自带回归语料与断言：改完脚本必跑 `python scripts/kb_slim_selftest.py`（fixtures 里故意埋的问题必须全部被重新抓出）。

## 十二、分层审查：不同频率查不同东西

一次全量审查既贵又慢，结论还很快过期。借汽车业**分层过程审核（LPA / AIAG CQI-8）**的思路：同一件事由不同层级、按不同频率各查一遍，互相兜底。

| 层 | 何时 | 谁 | 查什么 | 产出 |
|---|---|---|---|---|
| **L1 机检** | 每次改动 | 钩子 / 脚本 | 结构类：死链、孤儿、编码、脱敏、孤本引用、大文件 | 通过 / **阻断** |
| **L2 语义** | 每次任务 | AI | 21 条判定式 | 候选清单（人拍板） |
| **L3 体系** | 月 / 季 | 人 + AI | 口径是否传到所有仓、内容是否放对层、制度是否还在起作用 | 处置清单 |

> LPA 有两条经验值得直接抄：
> **一、多数失败不是未知风险，是"已知风险没被一致地控制"**——判定式写好了 ≠ 查过了；
> **二、清单本身不值钱，一致的观察 + 快速纠偏才值钱**。
> 防"打勾敷衍"：L1 用钩子强制（不过就不许提交）；L2 必须产出清单，**空清单也要声明"查过 N 条"**。

## 十三、半自动：把"判断"拆成检出与裁决

说"这条没法自动化"，通常是 **裁决** 没法自动，但 **检出** 可以。
半自动 = 机器出候选（召回优先）+ 人裁决（精度靠人）+ 每条给足证据。

把 21 条判定式逐条标注属性，就知道每条到底能自动到什么程度。**机检必须给出对应实现（最后一列），宣称即实现**——v1.5.3 前曾把 #6/#13 标成机检却无对应规则，撞了自家案例库"宣称≠实现"，已纠正：

| # | 判定式 | 检出 | 裁决 | 对应机检 |
|---|--------|------|------|---------|
| 1 | 单一事实来源 | 候选 | 人 | — |
| 2 | 放对层 | 候选 | 人 | — |
| 3 | 双写消除 | 候选 | 人 | — |
| 4 | 旧版清退 | 机检 | 建议 | S4 同名聚类 |
| 5 | 合并语义重复 | 候选 | 人 | — |
| 6 | 判定式短句 | 候选 | 人 | —（"算不算长文"的阈值需语义判断） |
| 7 | 孤本保护 | 机检 | 自动 | 删前 grep 引用（S8 是其反向体检） |
| 8 | 平台私有清退 | 手动 | 人 | — |
| 9 | 命名笼统=变相加层 | 机检 | 建议 | S6 |
| 10 | 台账不搬流水 | 机检 | 人 | S10 |
| 11 | 头注不堆叠历史 | 机检 | 自动 | S5 |
| 12 | 操作细节不属于规范 | 候选 | 人 | — |
| 13 | 下沉后只留一行指针 | 候选 | 人 | —（"半截说明"要读上下文才定） |
| 14 | 细则并入原则 | 手动 | 人 | — |
| 15 | 规则单源化 | 候选 | 人 | — |
| 16 | 去重复强调 | 候选 | 人 | — |
| 17 | 无人用的删除 | 手动 | 人 | — |
| 18 | 孤儿清理 | 机检 | 建议 | S8 + S9 |
| 19 | 不规定 AI 本来就会做的事 | 候选 | 人 | — |
| 20 | 冻结不删 | 手动 | 人 | — |
| 21 | 旧措辞残留 | 候选 | 建议 | 全库 grep 旧措辞 |

**检出**：机检 = 脚本可直接判（表内须指明对应规则）；候选 = 能排出可疑项但定不了；手动 = 得读才知道
**裁决**：自动 = 脚本可直接拦；建议 = 脚本给方案、人一键采纳；人 = 必须人拍板

> 统计下来：**6 条可机检、11 条能出候选、4 条需要读**——机检加候选仍占 17/21，真正要通读原文的只有 4 条。
> 所谓"不能自动化"，大多只是**不能自动裁决**——先把检出和裁决拆开，再看能自动化多少。
>
> 精益里这叫 **自働化（Jidoka）**，不是"全自动化"：异常 **自动检出 → 自动停止 → 呼人处理**。
> **判定权留给人是设计，不是缺陷。** 追求全自动反而去掉了人的判断。

## 十四、统一流程：统一契约，不统一实现

不同 AI 审查方法不同没关系——**只要每一步的输入输出固定，结果就可比、可验收、可回归**。

```
scan → detect → triage → apply → verify
  ↓       ↓        ↓        ↓       ↓
 清单    候选     裁决      补丁    复检
```

| 步 | 产出 | 必须包含 |
|---|---|---|
| scan 盘点 | inventory | 路径 / 行数 / 该仓有没有 git |
| detect 检出 | findings | **规则 ID + 位置 + 证据 + 置信度** |
| triage 裁决 | decisions | 采纳 / 驳回 / **暂缓** + 理由 |
| apply 执行 | patch | 只出 diff，不直接改文件 |
| verify 复检 | report | 重跑 scan/detect，对比前后规模 |

三个直接好处：

1. **人只在 triage 打勾**——不必陪跑全程
2. **不同 AI 的 findings 可横向对比**——谁的召回高一目了然
3. **可回归测试**——攒一批"已知有问题的样本"，改完脚本跑一遍看还报不报得出来（这才是"真空绿"的正解）

> 流程固化成**产物**，就不依赖每个 AI 自觉照做——
> **制度靠 SOP 落地，SOP 靠防呆兜底，防呆靠机检自证。**
